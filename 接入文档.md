# PubeasySDK iOS 接入文档

## 概述

PubeasySDK 是一个iOS平台的广告聚合SDK，基于TradPlus框架构建，支持多种广告形式包括横幅广告、插屏广告、原生广告、激励视频广告和开屏广告。SDK以二进制xcframework形式分发，方便快速集成。

## 支持的广告类型

- **横幅广告 (Banner)** - 页面底部或顶部的固定位置广告
- **插屏广告 (Interstitial)** - 全屏展示的广告
- **原生广告 (Native)** - 与应用内容融合的自定义样式广告
- **激励视频广告 (Rewarded)** - 用户观看完整视频后获得奖励的广告
- **开屏广告 (Splash)** - 应用启动时展示的全屏广告

## 系统要求

- iOS 12.0+
- Xcode 11.0+
- Swift 5.0+ 或 Objective-C

## 快速集成

### 1. CocoaPods 集成 (推荐)

在项目的 `Podfile` 中添加：

```ruby
pod 'PubyYanSDK', '~> 0.0.5'
```

然后执行：

```bash
pod install
```

### 2. 手动集成

1. 下载最新版本的 SDK：[PubYanSDK_0.0.5.zip](https://github.com/guangyao/pbsdk/releases/download/v0.0.5/PubYanSDK_0.0.5.zip)

2. 解压后将以下文件添加到项目中：
   - `PubeasySDK.xcframework`
   - `TradPlusFrameworks/TradPlusAds/TradPlusAds.framework`
   - `TradPlusFrameworks/TradPlusAds/TPExchange.framework`
   - `TradPlusFrameworks/TPCross/TPCrossAdapter.framework`

3. 添加资源文件：
   - `TradPlusFrameworks/TradPlusAds/TradPlusAds.bundle`
   - `TradPlusFrameworks/TradPlusAds/TradPlusADX.bundle`
   - `Assets/PrivacyInfo.xcprivacy`

4. 在项目设置中添加必要的系统框架：
   - `UIKit`
   - `Foundation`
   - `AdSupport`
   - `CoreTelephony`

5. 添加系统库：
   - `libz.tbd`
   - `libsqlite3.tbd`
   - `libc++.tbd`

6. 在 Build Settings 中设置：
   - Other Linker Flags: `-ObjC -all_load`

## 基础使用

### 1. SDK 初始化

在 `AppDelegate.m` 中导入SDK并初始化：

```objective-c
#import <PubeasySDK/PubeasySDK.h>

@implementation AppDelegate

- (BOOL)application:(UIApplication *)application didFinishLaunchingWithOptions:(NSDictionary *)launchOptions {
    
    // 初始化SDK
    [PubeasyManager initSDK:@"your_app_id" completionBlock:^(NSError * _Nullable error) {
        if (error) {
            NSLog(@"SDK初始化失败: %@", error.localizedDescription);
        } else {
            NSLog(@"SDK初始化成功");
        }
    }];
    
    return YES;
}

@end
```

### 2. 可选配置

```objective-c
// 设置可选参数
NSDictionary *config = @{
    @"userId": @"user123",
    @"custom_param": @"value"
};
[PubeasyManager registerWithOptionalData:config];
```

## 广告接入详解

### 横幅广告 (Banner)

```objective-c
#import <PubeasySDK/PubeasyAdBanner.h>

@interface ViewController () <PubeasyAdBannerDelegate>
@property (nonatomic, strong) PubeasyAdBanner *bannerAd;
@end

@implementation ViewController

- (void)loadBannerAd {
    // 创建横幅广告
    self.bannerAd = [[PubeasyAdBanner alloc] initWithFrame:CGRectMake(0, 0, 320, 50)];
    self.bannerAd.delegate = self;
    
    // 设置广告位ID
    [self.bannerAd setAdUnitID:@"your_banner_unit_id"];
    
    // 设置横幅尺寸（可选）
    [self.bannerAd setBannerSize:CGSizeMake(320, 50)];
    
    // 添加到视图
    [self.view addSubview:self.bannerAd];
    
    // 加载广告
    [self.bannerAd loadAdWithSceneId:@"scene_id"];
}

#pragma mark - PubeasyAdBannerDelegate

- (void)pubeasyBannerAdLoaded:(NSDictionary *)adInfo {
    NSLog(@"横幅广告加载成功");
}

- (void)pubeasyBannerAdLoadFailWithError:(NSError *)error adInfo:(NSDictionary *)adInfo {
    NSLog(@"横幅广告加载失败: %@", error.localizedDescription);
}

- (void)pubeasyBannerAdImpression:(NSDictionary *)adInfo {
    NSLog(@"横幅广告展示");
}

- (void)pubeasyBannerAdClicked:(NSDictionary *)adInfo {
    NSLog(@"横幅广告被点击");
}

@end
```

### 插屏广告 (Interstitial)

```objective-c
#import <PubeasySDK/PubeasyAdInterstitial.h>

@interface ViewController () <PubeasyAdInterstitialDelegate>
@property (nonatomic, strong) PubeasyAdInterstitial *interstitialAd;
@end

@implementation ViewController

- (void)loadInterstitialAd {
    // 创建插屏广告
    self.interstitialAd = [[PubeasyAdInterstitial alloc] init];
    self.interstitialAd.delegate = self;
    
    // 设置广告位ID
    [self.interstitialAd setAdUnitID:@"your_interstitial_unit_id"];
    
    // 加载广告
    [self.interstitialAd loadAd];
}

- (void)showInterstitialAd {
    if (self.interstitialAd.isAdReady) {
        [self.interstitialAd showAdWithSceneId:@"scene_id"];
    }
}

#pragma mark - PubeasyAdInterstitialDelegate

- (void)pubeasyInterstitialAdLoaded:(NSDictionary *)adInfo {
    NSLog(@"插屏广告加载成功");
}

- (void)pubeasyInterstitialAdLoadFailWithError:(NSError *)error adInfo:(NSDictionary *)adInfo {
    NSLog(@"插屏广告加载失败: %@", error.localizedDescription);
}

- (void)pubeasyInterstitialAdImpression:(NSDictionary *)adInfo {
    NSLog(@"插屏广告展示");
}

- (void)pubeasyInterstitialAdClicked:(NSDictionary *)adInfo {
    NSLog(@"插屏广告被点击");
}

- (void)pubeasyInterstitialAdDismissed:(NSDictionary *)adInfo {
    NSLog(@"插屏广告关闭");
}

@end
```

### 激励视频广告 (Rewarded)

```objective-c
#import <PubeasySDK/PubeasyAdRewarded.h>

@interface ViewController () <PubeasyAdRewardedDelegate>
@property (nonatomic, strong) PubeasyAdRewarded *rewardedAd;
@end

@implementation ViewController

- (void)loadRewardedAd {
    // 创建激励视频广告
    self.rewardedAd = [[PubeasyAdRewarded alloc] init];
    self.rewardedAd.delegate = self;
    
    // 设置广告位ID
    [self.rewardedAd setAdUnitID:@"your_rewarded_unit_id"];
    
    // 设置服务器端验证（可选）
    [self.rewardedAd setServerSideVerificationOptionsWithUserID:@"user123" customData:@"custom_data"];
    
    // 加载广告
    [self.rewardedAd loadAd];
}

- (void)showRewardedAd {
    if (self.rewardedAd.isAdReady) {
        [self.rewardedAd showAdWithSceneId:@"scene_id"];
    }
}

#pragma mark - PubeasyAdRewardedDelegate

- (void)pubeasyRewardedAdLoaded:(NSDictionary *)adInfo {
    NSLog(@"激励视频广告加载成功");
}

- (void)pubeasyRewardedAdLoadFailWithError:(NSError *)error adInfo:(NSDictionary *)adInfo {
    NSLog(@"激励视频广告加载失败: %@", error.localizedDescription);
}

- (void)pubeasyRewardedAdImpression:(NSDictionary *)adInfo {
    NSLog(@"激励视频广告展示");
}

- (void)pubeasyRewardedAdReward:(NSDictionary *)adInfo {
    NSLog(@"用户获得奖励");
    // 在这里给用户发放奖励
}

- (void)pubeasyRewardedAdDismissed:(NSDictionary *)adInfo {
    NSLog(@"激励视频广告关闭");
}

@end
```

### 开屏广告 (Splash)

```objective-c
#import <PubeasySDK/PubeasyAdSplash.h>

@interface AppDelegate () <PubeasyAdSplashDelegate>
@property (nonatomic, strong) PubeasyAdSplash *splashAd;
@end

@implementation AppDelegate

- (void)loadSplashAd {
    // 创建开屏广告
    self.splashAd = [[PubeasyAdSplash alloc] init];
    self.splashAd.delegate = self;
    
    // 设置广告位ID
    [self.splashAd setAdUnitID:@"your_splash_unit_id"];
    
    // 加载广告 (在主窗口上展示)
    UIView *bottomView = nil; // 可选的底部视图
    [self.splashAd loadAdWithWindow:self.window bottomView:bottomView];
}

#pragma mark - PubeasyAdSplashDelegate

- (void)pubeasySplashAdLoaded:(NSDictionary *)adInfo {
    NSLog(@"开屏广告加载成功");
    // 广告加载成功，可以展示
    [self.splashAd showWithSceneId:@"scene_id"];
}

- (void)pubeasySplashAdLoadFailWithError:(NSError *)error adInfo:(NSDictionary *)adInfo {
    NSLog(@"开屏广告加载失败: %@", error.localizedDescription);
    // 进入主界面
    [self enterMainViewController];
}

- (void)pubeasySplashAdDismissed:(NSDictionary *)adInfo {
    NSLog(@"开屏广告关闭");
    // 进入主界面
    [self enterMainViewController];
}

- (void)enterMainViewController {
    // 实现进入主界面的逻辑
}

@end
```

### 原生广告 (Native)

```objective-c
#import <PubeasySDK/PubeasyAdNative.h>

@interface ViewController () <PubeasyAdNativeDelegate>
@property (nonatomic, strong) PubeasyAdNative *nativeAd;
@end

@implementation ViewController

- (void)loadNativeAd {
    // 创建原生广告
    self.nativeAd = [[PubeasyAdNative alloc] init];
    self.nativeAd.delegate = self;
    
    // 设置广告位ID
    [self.nativeAd setAdUnitID:@"your_native_unit_id"];
    
    // 设置模版渲染尺寸（可选）
    [self.nativeAd setTemplateRenderSize:CGSizeMake(300, 200)];
    
    // 加载广告
    [self.nativeAd loadAd];
}

- (void)showNativeAd {
    if (self.nativeAd.isAdReady) {
        // 使用自定义渲染类展示
        [self.nativeAd showADWithRenderingViewClass:[CustomNativeAdView class] 
                                           subview:self.view 
                                           sceneId:@"scene_id"];
    }
}

#pragma mark - PubeasyAdNativeDelegate

- (void)pubeasyNativeAdLoaded:(NSDictionary *)adInfo {
    NSLog(@"原生广告加载成功");
    [self showNativeAd];
}

- (void)pubeasyNativeAdLoadFailWithError:(NSError *)error adInfo:(NSDictionary *)adInfo {
    NSLog(@"原生广告加载失败: %@", error.localizedDescription);
}

- (void)pubeasyNativeAdImpression:(NSDictionary *)adInfo {
    NSLog(@"原生广告展示");
}

- (void)pubeasyNativeAdClicked:(NSDictionary *)adInfo {
    NSLog(@"原生广告被点击");
}

@end
```

## 高级功能

### 自定义广告信息

```objective-c
// 设置自定义广告信息，会在回调中返回
self.bannerAd.customAdInfo = @{
    @"source": @"main_page",
    @"position": @"bottom"
};
```

### 本地配置参数

```objective-c
// 设置本地配置参数
self.bannerAd.localParams = @{
    @"key1": @"value1",
    @"key2": @"value2"
};
```

### 获取广告信息

```objective-c
// 获取准备就绪的广告信息
NSDictionary *readyAdInfo = [self.bannerAd getReadyAdInfo];

// 获取当前显示的广告信息
NSDictionary *currentAdInfo = [self.bannerAd getCurrentAdInfo];
```

### 场景ID管理

```objective-c
// 进入广告场景
[self.interstitialAd entryAdScenario:@"level_complete"];

// 在指定场景展示广告
[self.interstitialAd showAdWithSceneId:@"level_complete"];
```

## 测试和调试

### 1. 测试广告位ID

请联系技术支持获取测试用的广告位ID。

### 2. 调试模式

```objective-c
// 在开发阶段建议开启详细日志
// 可以在 info 回调中查看详细的广告信息
```

### 3. 错误处理

所有广告类型都有对应的错误回调方法，建议实现这些方法以便调试：

```objective-c
- (void)pubeasyBannerAdLoadFailWithError:(NSError *)error adInfo:(NSDictionary *)adInfo {
    NSLog(@"错误码: %ld", error.code);
    NSLog(@"错误信息: %@", error.localizedDescription);
    NSLog(@"广告信息: %@", adInfo);
}
```

## 隐私政策

SDK已包含隐私清单文件 `PrivacyInfo.xcprivacy`，符合苹果App Store的隐私要求。

## 常见问题

### 1. 编译错误

**问题**: 找不到头文件
**解决**: 确保正确导入了框架，并在 Build Settings 中设置了正确的 Header Search Paths

### 2. 运行时崩溃

**问题**: 启动时崩溃
**解决**: 检查是否正确设置了 Other Linker Flags: `-ObjC -all_load`

### 3. 广告不展示

**问题**: 广告加载成功但不展示
**解决**: 
- 检查广告位ID是否正确
- 确认网络连接正常
- 检查是否在主线程调用展示方法

### 4. 模拟器问题

**问题**: 在模拟器上无法运行
**解决**: 在 User-Defined 设置中添加 `EXCLUDED_ARCHS[sdk=iphonesimulator*] = arm64`

## 技术支持

如遇到集成问题，请联系：
- 邮箱: 976660988@qq.com
- GitHub: [https://github.com/guangyao/pbsdk](https://github.com/guangyao/pbsdk)

## 版本更新

当前版本：0.0.5

请定期检查更新以获得最新功能和bug修复。

---

*本文档最后更新：2025年*

